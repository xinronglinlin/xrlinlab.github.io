name: Mirror and publish xrlinlab.com

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - ".github/workflows/mirror-site.yml"

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  mirror:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Install HTTrack
        run: |
          sudo apt-get update
          sudo apt-get install -y httrack

      - name: Mirror current website
        run: |
          MIRROR_DIR="$RUNNER_TEMP/xrlinlab-mirror"
          SITE_DIR="$RUNNER_TEMP/xrlinlab-site"
          rm -rf "$MIRROR_DIR" "$SITE_DIR"
          mkdir -p "$MIRROR_DIR" "$SITE_DIR"

          httrack "https://www.xrlinlab.com/" \
            -O "$MIRROR_DIR" \
            "+*.xrlinlab.com/*" \
            "-*/wp-admin/*" \
            "-*/wp-login.php*" \
            "-*/cart/*" \
            "-*/checkout/*" \
            -v

          SOURCE_DIR="$MIRROR_DIR/www.xrlinlab.com"
          if [ ! -d "$SOURCE_DIR" ]; then
            SOURCE_DIR="$(find "$MIRROR_DIR" -maxdepth 1 -type d -name '*xrlinlab.com*' | head -n 1)"
          fi

          if [ -z "$SOURCE_DIR" ] || [ ! -d "$SOURCE_DIR" ]; then
            echo "Mirrored site directory not found."
            find "$MIRROR_DIR" -maxdepth 2 -type d -print
            exit 1
          fi

          rsync -a "$SOURCE_DIR/" "$SITE_DIR/"
          touch "$SITE_DIR/.nojekyll"

      - name: Save mirrored files to repository
        run: |
          SITE_DIR="$RUNNER_TEMP/xrlinlab-site"
          rsync -a --delete \
            --exclude=".git/" \
            --exclude=".github/" \
            --exclude="README.md" \
            "$SITE_DIR/" ./

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "No website changes detected."
          else
            git commit -m "Mirror xrlinlab.com as static HTML"
            git push
          fi

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: ${{ runner.temp }}/xrlinlab-site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
